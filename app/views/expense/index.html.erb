<div class="container">
  <div class="d-flex justify-content-between align-items-center mb-2">
    <%= link_to "🔙 Back", user_category_index_path(current_user), class: "back-btn btn btn-sm" %><br/>
    <p><strong><%= @category.name %> Category</strong></p>
  </div>

  <div class="card">
    <div class="card-body">
      <p>You have spent <strong><%= number_to_currency(@category.sum_amount, unit: "৳") %></strong> so far</p>
    </div>
  </div><br>

  <% @expenses.each do |expense| %>
    <div class="card mb-4">
      <div class="card-body d-flex align-items-center mx-4 justify-content-between">
        <h5 class="card-title mb-2 mx-2"> <%= expense.name.capitalize %> </h5>
        <p class="card-text time"><%= expense.created_at.strftime('%a %d %b, %Y') %></p>
        <h5 class="card-body m-6"> <%= number_to_currency(expense.amount, unit: "৳") %> </h5>
      </div>
    </div>
  <% end %>
</div>

<div class="category-div">
  <%= link_to 'Add new Expense', new_expense_path(category_id: @category.id), class: "btn new-category" %>
</div>